---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import BMICalculator from '../../components/BMICalculator.astro';
import ResultStudio from '../../components/ResultStudio.astro';
import HealthContent from '../../components/HealthContent.astro';
import LatestArticles from '../../components/LatestArticles.astro';
import DeepHealthSEO from '../../components/DeepHealthSEO.astro';
---

<Layout 
	title="Real BMI 계산기 | 정확한 BMI 및 BMR 도구"
	description="무료 온라인 BMI 계산기. Real BMI 계산기로 BMI, BMR 및 체성분을 즉시 계산하세요."
>
	<Navbar />
	<main class="pt-32 pb-20 px-6">
		<div class="max-w-4xl mx-auto text-center space-y-4 mb-16 animate-in">
			<div class="inline-flex items-center gap-2 px-3 py-1 rounded-lg bg-[var(--muted)] border border-[var(--border)] text-[10px] font-black uppercase tracking-[0.2em] text-[var(--accent)]">
				프리미엄 건강 분석
			</div>
			<h1 class="text-4xl md:text-5xl font-black tracking-tighter text-[var(--foreground)] uppercase">
				BMI 계산기 – Real BMI
			</h1>
			<p class="text-sm md:text-base text-[var(--muted-foreground)] leading-relaxed max-w-2xl mx-auto font-bold opacity-60">
				현대 건강 애호가를 위한 정밀 지표. 임상적으로 검증된 공식으로 계산됩니다.
			</p>
		</div>

		<div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-8 items-stretch mb-32 relative z-10">
			<BMICalculator />
			<ResultStudio />
		</div>

		<LatestArticles />
		<HealthContent />
		<DeepHealthSEO />
	</main>
</Layout>

<style is:global>
	@keyframes fadeIn {
		from { opacity: 0; transform: translateY(10px); }
		to { opacity: 1; transform: translateY(0); }
	}
	.animate-in {
		animation: fadeIn 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
	}
</style>
